<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp"
    tools:context=".NoInternetActivity">

    <!-- WiFi Off Icon -->
    <ImageView
        android:id="@+id/noInternetIcon"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:contentDescription="No Internet"
        android:src="@drawable/ic_no_internet"
        android:alpha="0" />

    <!-- Title -->
    <TextView
        android:id="@+id/noInternetTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:fontFamily="sans-serif-bold"
        android:text="No Internet Connection"
        android:textColor="@color/text_primary"
        android:textSize="22sp"
        android:alpha="0" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/noInternetSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="Please check your Wi-Fi or mobile data connection and try again."
        android:textColor="@color/text_secondary"
        android:textSize="15sp"
        android:alpha="0" />

    <!-- Retry Button -->
    <Button
        android:id="@+id/retryButton"
        android:layout_width="200dp"
        android:layout_height="52dp"
        android:layout_marginTop="36dp"
        android:backgroundTint="@color/primary"
        android:fontFamily="sans-serif-medium"
        android:text="Retry"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:alpha="0"
        style="@style/Widget.Material3.Button" />

    <!-- Auto-connecting hint -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ProgressBar
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/text_hint" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:fontFamily="sans-serif"
            android:text="Will auto-connect when network is available"
            android:textColor="@color/text_hint"
            android:textSize="12sp" />

    </LinearLayout>

</LinearLayout>
